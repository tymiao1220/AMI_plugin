import router from 'girder/router';
// import AMIViewer from './views/AMIViewerSEG';
// import FileModal from 'girder/models/FileModel';
router.route('plugins/AMI/:id/view', 'histogramConfig', function (fileId, params) {
    // reconstructed url array
});
